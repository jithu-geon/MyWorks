import { GlobalNxArgs } from './nx';
import * as yargs from 'yargs';
export interface YargsAffectedOptions extends yargs.Arguments {
}
export interface AffectedOptions extends GlobalNxArgs {
    parallel: boolean;
    maxParallel: number;
    untracked: boolean;
    uncommitted: boolean;
    all: boolean;
    base: string;
    head: string;
    exclude: string[];
    files: string[];
    onlyFailed: boolean;
    'only-failed': boolean;
    'max-parallel': boolean;
}
export declare function affected(command: string, parsedArgs: any, args: string[]): void;
